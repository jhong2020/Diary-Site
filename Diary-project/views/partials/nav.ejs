<nav>
    <ul>
        <li><a href="/"><img src="/main.png" alt="마이페이지" width="30" height="30"></a></li>
        <li><a href="/list"><img src="/list.png" alt="마이페이지" width="30" height="30"></a></li>
        <li><a href="/write"><img src="/write.png" alt="마이페이지" width="30" height="30"></a></li>
        <li><a href="/MyPage"><img src="/mypage.png" alt="마이페이지" width="30" height="30"></a></li>
        <li><a href="/logout"><img src="/logout.png" alt="마이페이지" width="30" height="30"></a></li> <!-- 로그아웃 버튼 -->
    </ul>
    <!-- 네비게이션 바 -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <form class="d-flex ms-auto search-form" action="/search" method="GET"> <!-- 검색 기능을 위한 폼 -->
                <input class="form-control me-2 search-input" type="search" placeholder="검색어 입력" aria-label="Search" name="keyword">
                <button class="btn btn-outline-success search-button" type="submit">검색</button>
            </form>
            <div style="display: flex; align-items: center;">
                <span style="color: #333; margin-right: 10px;">현재 시간</span>
                <span id="local-time" style="color: #007bff; font-weight: bold; font-size: 1.2em;"></span> <!-- 로컬 시간 표시 -->
            </div>
        </div>
    </nav>
</nav>

<style>
    /* 검색 컨테이너 스타일 */
    .search-form {
        margin-left: auto; /* 검색 폼을 오른쪽으로 이동 */
    }

    /* 검색 입력 필드 스타일 */
    .search-input {
        border-radius: 20px; /* 검색 입력 필드를 둥글게 만듦 */
    }

    /* 검색 버튼 스타일 */
    .search-button {
        border-radius: 20px; /* 검색 버튼을 둥글게 만듦 */
        margin-left: 10px; /* 검색 버튼과 입력 필드 사이 간격 추가 */
    }
</style>

<script>
    // 로컬 시간 업데이트 함수
    function updateLocalTime() {
        const now = new Date();
        const hours = now.getHours().toString().padStart(2, '0');
        const minutes = now.getMinutes().toString().padStart(2, '0');
        const seconds = now.getSeconds().toString().padStart(2, '0');
        const timeString = `${hours}:${minutes}:${seconds}`;
        document.getElementById('local-time').textContent = timeString;
    }

    // 매 초마다 로컬 시간 업데이트
    setInterval(updateLocalTime, 1000);

    // 초기 로컬 시간 업데이트
    updateLocalTime();
</script>
